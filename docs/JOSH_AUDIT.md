# Josh's Games — Difficulty & Engagement Audit (for the parent)

*Auditor lens: Montessori-informed, calibrated to Josh (4.5) per [`JOSH_PROFILE.md`](../JOSH_PROFILE.md).
77 registered games across 8 files. Snapshot: July 2026. This is an honest
assessment of where games are too easy/boring, not a status brag — it exists to
steer the next round of improvements (see RULE 7 in CLAUDE.md).*

> **✅ Applied so far (safe/additive — nothing removed):** the "make challenge
> games scale" fixes from rec #1/#3/#6 — `order-num` now varies + climbs into the
> teens (was always 1-2-3-4); `order-size` grades 3→4→5; `place-value` climbs
> toward 99; `big-add` introduces carries in later rounds; `skip-hop` runs
> lengthen 5→7. Also: `digraph` now speaks its word; `magnet` coin/key falsehood
> fixed; Dot-to-Dot reveal fixed.
> **Still open (need a design call — they consolidate or rework existing play):**
> scaling/3-bin variants for the 7 sorters; adding real-skill layers to 3 of the 5
> tap-to-fill co-ops; a "Write My Name" (J-O-S-H) tracer; making `sight-word`
> audio-only; a single-continent-map mode; a plane-shape geometry game.

## 1. Headline verdict

The collection is broad, genuinely no-fail, nicely personalized, and **strongest
exactly where Josh is strongest** — the Find category (games that scale and theme
to his loves) and an unusually deep co-op bench (his #1 engagement lever). Nearly
every *headline* math working-edge has a dedicated, well-built game. **But it runs
easier than the 70/30 challenge/confidence target — realistically ~55–60% true
challenge — for two structural reasons, not because any single game is broken.**

1. **Almost nothing scales within a session.** Only the Find games and
   `number-muncher` get harder each round; the other ~50 multi-round games play
   round 5 at exactly round 1's difficulty. A curious 4.5-year-old who "gets it"
   in ten seconds still grinds four identical rounds.
2. **The confidence side is inflated by near-identical low-demand clones** — 7
   science sorters that are the same tap-a-bin, 5 co-ops that are the same
   take-turns-tap-to-fill-a-meter, and ~5 count-to-10 games — rather than a small,
   deliberate confidence slice.

The highest-leverage fix is **not** "add more games"; it's **make the challenge
games ramp, and consolidate/deepen the easy clones.**

## 2. Game buckets

### 🟥 Too easy / boring — needs work
| Game | Why it under-shoots |
|---|---|
| `science-sort`, `color-sort`, `law-sort`, `day-night`, `hot-cold`, `magnet-magic`, `blue-planet` | **Seven identical `[M]` tap-a-bin sorters**; round 1 = round 6, and the 2-bin ones are a 50/50 guess. Fine as a *small* fresh domain — seven is the biggest drag on the mix. |
| `order-num` | **Weakest game in the set.** Always renders **1-2-3-4** to tap in order — identical every round, every session. |
| `order-size` | Only 3 sizes to grade; grading sizes is `[M]` → a 10-second task. |
| `shadow-match` | Distinct geometric silhouettes (star vs circle vs heart) — unmistakable, no real discrimination. |
| `which-more` | Countable dot groups 1-7; for a child who's mastered "quantify 1-10" this plays `[M]`. |
| `sight-word` | **Shows the target word AND asks to tap the identical one — a non-reader passes by shape-matching without reading.** It's the only sight-word game, on a live `[W]` edge. |
| `count-feed`, `number-match` | Count-to-≤10 confidence, no scaling; overlaps with ~5 other count-to-10 games. |
| `first-sound`, `missing-letter`, `letter-match` | Legit `[M]` confidence, but the literacy strand skews confidence-heavy. |
| `team-hop`, `team-build`, `team-count`, `team-rocket`, `team-bridge` | **Five near-identical co-ops** (take turns tapping until a meter fills). Turn-taking is real value; the *cognitive* task is empty. |
| `peekaboo`, `balloon`, `animals` | Object-permanence / cause-effect — toddler-leaning for 4.5. Keep as delight. |

### 🟩 Well-pitched — challenge lands right
- **Math:** `place-value`, `make-ten`, `big-add`, `number-muncher`, `skip-hop`, `coin-shop`, `piggy-bank`, `take-away`, `clock`, `set-clock`, `add-up`
- **Literacy:** `rhyme`, `rhyme-train`, `digraph-finish`, `read-zap`, `build-word`, `letter-maker`
- **Logic:** `who-is-it` (real 2-clue deduction + round-1 demo), `birds-eye` (top-down spatial — nothing else touches it), `picture-squares`, `story-order`, `spot-diff`, `what-next`, `memory`
- **Find (his favorite):** `find-hero`, `find-crowd`, `find-count`, `rescue`, `web-reveal`, `big-red-one`, `find-twins`, `i-spy`, `dot-dot`, `tic-tac-toe` — **all scale each round; the model the rest of the site should copy.**
- **Science:** `solid-match`, `landform-maker`, `continent-match`
- **Calm/co-op:** `trace-path`, `team-sound-hunt`, `team-treasure`, `memory-together`, `breathe`, `certificate`
- **Fun:** `grow`, `thwip-web`, `color-number`, `music-pad`, `bubbles`

### 🟨 Good idea, capped too low / flat
| Game | Ceiling problem |
|---|---|
| `big-add` | **Never regroups**, caps ~48. 2-digit addition is his *headline* edge — it should reach a carry. |
| `place-value` | Caps at **11-39**, yet his ten-board-to-100 is **mastered** — under-shoots his own ceiling. |
| `skip-hop` | Always a 5-term run with one gap; only 2s/5s/10s. Deserves longer runs / two gaps / 3s. |
| `make-ten` | Bonds to 10 only; bonds-to-20 is the natural ramp. |
| `continent-match` | The animal **sits on its home continent**, so it collapses to "tap the color the animal is on." His edge is the *six individual* maps. |
| `digraph` | 2-bin (sh/ch) = 50/50; `digraph-finish`'s 3-way is strictly better. |
| `who-is-it` | Could add a 3-attribute round. |
| `memory` | Fixed 4 pairs; could grow to 6. |
| `letter-maker` | Traces L/V/O/C/U — **not his name.** |
| the 7 sorters | Profile explicitly says science `[M]` "→ push to trickier examples." They never do. |

## 3. Difficulty balance vs the 70/30 target

Of the ~72 teaching/challenge games (excluding ~5 pure delight/calm toys):
- **~55%** land at a true `[W]`/`[P]` challenge
- **~25%** are challenge-*intent* games that under-shoot to `[M]` (the 7 sorters, `order-num`/`order-size`/`shadow-match`, `which-more`, the count-to-10 cluster, weak `sight-word`)
- **~7%** cognitively-empty co-ops (5 tap-to-fill)
- **~13%** legitimate confidence/calm slice

**Verdict: ~55–60% challenge / 40–45% confidence — under the 70% target, skewed easy.**
The miss is *concentrated*: removing the drag from the 7 identical sorters + 5
identical co-ops + `order-num` jumps the ratio most of the way to target. It's a
**distribution problem (too many clones of a few easy mechanics) plus a scaling
problem (challenge games don't ramp)** — not a shortage of good hard games.

## 4. Coverage gaps — thin `[W]` working edges
- **Sight words** — one game, and it shape-matches instead of reading. *Weakest-covered edge.*
- **Writing his name** — `letter-maker` traces L/V/O/C/U; **no game traces J-O-S-H.** *Gap.*
- **Count by 2s/5s** — `skip-hop` only, flat, no 3s. *Thin.*
- **2-digit addition** — `big-add` only, never regroups. *Thin at the top.*
- **The six individual continent maps** — `continent-match` mixes all 7 with a self-check giveaway. *Gap.*
- **Geometry cabinets / plane shapes** — only 3D solids covered. *Gap.*
- **Sharing / conflict-resolution SEL** — implied by turn-taking only. *Thin.*
- **Over-served:** 7 `[M]` sorters, 5 tap-to-fill co-ops, ~5 count-to-10 games.

## 5. Prioritized recommendations (most impactful first)

1. **Make challenge games *scale*, using the Find category (`size = 9 + round*2`)
   as the template.** Add per-round ramps: `big-add` introduces a carry by round 4;
   `place-value` climbs past 40→99; `skip-hop` lengthens the run / hides two terms;
   the sorters move to trickier examples; `make-ten` graduates to 20. *One change
   pattern upgrades many existing `[W]` games at once — biggest bang for the buck.*
2. **Rebalance the two over-weighted easy clusters instead of adding games.**
   (a) Let the 7 sorters scale to trickier/edge items; give one or two a 3-bin
   variant. (b) Layer a real skill onto 3 of the 5 tap-to-fill co-ops (as
   `team-sound-hunt`/`team-treasure` already do). *Fastest route from ~57% to ~70%
   challenge, and it turns his #1 engagement lever into a learning lever.*
3. **Fix `order-num` (and deepen `order-size`).** Randomize the run (4-7, or 12-15,
   or a missing-number-in-sequence); bump `order-size` to 4-5 items. *Near-zero
   effort, removes the worst offender, stretches number-sequence past 10.*
4. **Add a "Write My Name" tracer (J-O-S-H)** + friends' names. *Hits his literal
   writing edge AND the cheapest high-affect personalization lever.*
5. **Make `sight-word` a real reading task** — speak the word only (or a short
   audio-supported phrase with the word missing) so he recognizes it rather than
   shape-matching. *Converts an `[M]`-plays game into a genuine `[W]` one.*
6. **Raise the ceiling on 2-digit addition and place value** — `big-add` reaches a
   regroup in later rounds; `place-value` builds toward 99. *His headline math edges,
   both currently stopping short of his own mastered range.*
7. **Cover thin geography/geometry edges** — a `continent-match` mode that names one
   continent without the animal-on-the-blob giveaway; a plane-shape game alongside
   `solid-match`.
8. **Turn co-op into a learning multiplier** — add co-op *layers* to existing
   challenge games (2-player `big-add` or `rhyme` race, take-turns `picture-squares`)
   rather than a 6th meter-fill.
9. **(Minor) Age-up the toddler-leaning delight** — a light counting/naming beat on
   `peekaboo`/`balloon`/`animals`.

---

*What's genuinely working and should be protected:* the Find category (scales +
themed + his favorite), the depth of co-op, the `[W]` logic quartet (`who-is-it`,
`birds-eye`, `picture-squares`, `story-order`), the new money games closing the old
coin gap, and the strong transformation-over-decoration design in `number-builder`,
`grow`, `landform-maker`, `piggy-bank`, `web-reveal`, and `thwip-web`. The site's
problem isn't quality — it's that its many good hard games stay at one difficulty,
and a handful of easy mechanics are cloned too many times.
